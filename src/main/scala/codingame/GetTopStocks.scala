package codingame

object GetTopStocks {
  def getTopStocks(stocks: Array[String], prices: Array[Array[Int]]): Array[Int] = {
    for (i <- 0 until (stocks.length - 1)) {
      for (j <- 0 until (prices.length - 1)) {
        for (k <- 0 until (prices.length - 1)) {

        }
      }
    }
    null
  }
}
